# Task Memory

- Purpose: Personal knowledge base for helping future LLM agents retrieve higher-quality UI/design and task workflow context.
- Key files: `README.md`, `backend_api/app/main.py`, `backend_api/README.md`, `domains/ui_design/CURATION_GUIDE.md`, `domains/agent_workflow/CURATION_GUIDE.md`, `common/schemas/ui_reference.schema.json`, `common/schemas/agent_workflow_reference.schema.json`.
- UI scripts: `scripts/ui_design/normalize_reference.py`, `scripts/ui_design/make_chunks.py`, `scripts/ui_design/build_sqlite_index.py`, `scripts/ui_design/search_ui.py`.
- Workflow scripts: `scripts/agent_workflow/normalize_reference.py`, `scripts/agent_workflow/make_chunks.py`, `scripts/agent_workflow/build_sqlite_index.py`, `scripts/agent_workflow/search_workflow.py`.
- Automation scripts: `scripts/automation/normalize_reference.py`, `scripts/automation/make_chunks.py`, `scripts/automation/build_sqlite_index.py`, `scripts/automation/search_automation.py`.
- UI asset scripts: `scripts/ui_assets/ingest_assets.py`; default import copies files into `domains/ui_assets/raw/review_required/*` and writes metadata to `domains/ui_assets/processed/metadata/*.json`.
- Agent usage: `AGENT_USAGE.md` plus local API first. Trigger phrases such as `根据我的数据库` / `use my database` require `GET http://127.0.0.1:8765/health` then `POST http://127.0.0.1:8765/brief`; GitHub is backup/inspection/sync only, not runtime retrieval.
- Default brief CLI: `scripts/brief.py "<task>" --ui 8 --workflow 2 --automation 0 --assets 10` for UI/frontend tasks. Use `--ui 2 --workflow 4 --automation 8 --assets 0` for browser automation/RPA/upload, and `--ui 2 --workflow 6 --automation 2 --assets 0` for general coding/agent workflow.
- API: `cd E:\DataBase\backend_api && python -m uvicorn app.main:app --host 127.0.0.1 --port 8765`; endpoints: `/health`, GET/POST `/brief`, `/ui/search`, `/ui/references`, `/ui/reference/{record_id}`, `/workflow/search`, `/workflow/references`, `/workflow/reference/{record_id}`, `/automation/search`, `/automation/references`, `/automation/reference/{record_id}`.
- Startup/test: Use PowerShell `-NoProfile` because the default conda hook emits GBK encoding errors in this environment. In Bash/Git Bash, use forward slashes for Windows paths.
- Last completed: Added POST `/brief` and ranked chunk selection. Ranking boosts implementation/parameters, layout, interaction, accessibility, evidence strength, prompt_tags, and exact query coverage. Brief now preserves at least one or two chunks per derived UI query before score-filling the rest.
- Verification: `backend_api/app/main.py` passed `py_compile`; direct `build_brief_response` returned balanced chunks for Liquid Glass, dark neon, website reveal, notification, and data visualization; temporary HTTP POST `/brief` returned expected queries, 10 UI chunks, 6 workflow chunks, and first UI chunk `implementation-css-liquid-glass-parameter-ranges-implementation`.
- Optional UI style: Liquid Glass means thick frosted blur, translucent capsule controls, glossy specular highlights, chromatic edge glow, soft refractive shadows, and luminous buttons/inputs/cards/modals. Use only when explicitly requested or clearly suitable.
- Optional motion source: Swishy.ai is stored as a motion taxonomy/pattern reference for kinetic typography, notification/message stacks, website reveal blur, data visualization motion, and device/product showcase. Do not copy templates, code, video output, or proprietary assets.
- Last inspected: 2026-04-25; usable DB counts are UI 32 references / 192 chunks, workflow 5 references / 33 chunks, automation 11 references / 77 chunks.
- Last completed: Reverted saved brief artifacts and removed the local `D:\ai-ui-test` Liquid Glass case-study reference after user feedback that case retention and screenshot-driven automation were overkill. `/brief` now defaults `automation_limit` to 0; ordinary frontend tasks should use UI/workflow chunks only. Automation remains available through `/automation/*` and `/brief` when callers explicitly set `automation_limit > 0` for browser automation, upload, CDP, screenshot, or verification work.
- Last completed: Added `AGENT_USAGE.md` and `scripts/brief.py` so other agents can fetch a concise brief without memorizing HTTP. Verified normal frontend brief returns no automation chunks by default and explicit automation brief returns Playwright chunks.
- Last completed: Added `visual-style-ambient-dynamic-background-liquid-glass` to UI references for high-end Liquid Glass ecological/dynamic scene backgrounds. `/brief` now maps dynamic background, scenery, ecology, ambient, aurora, caustics, and related Chinese terms to `ambient dynamic background liquid glass`.
- Last completed: Added three dynamic background subpatterns: `visual-style-aurora-haze-background`, `visual-style-water-caustics-liquid-glass-background`, and `visual-style-data-current-particle-background`. `/brief` maps aurora/mist, water caustics/refractive light, and data-current/particle terms to these references. `/ui/search` now ranks UI candidates over a wider candidate set before returning results.
- Last completed: Added new `ui_assets` domain for real UI素材/assets, plus `common/schemas/ui_asset.schema.json` and `scripts/ui_assets/ingest_assets.py`. Domain separates design rules (`ui_design`) from asset files (`ui_assets`). Ingest defaults to `usage_policy=review_required` so unknown web downloads are not treated as direct-use assets.
- Last completed: Imported selected `F:\materials` assets into `ui_assets`: 13 `motion_reference` videos and 46 `screenshot_reference` images as `inspiration_only`; 184 font files as `review_required`; 12 large icon/UI-kit/Lottie repositories as collection-level metadata. Total `processed/metadata/*.json`: 255. Raw copied files exclude `.git` repositories.
- Last completed: Added lightweight API request logging to `runtime/logs/api_requests.jsonl`. Logged endpoints include health, UI/workflow/automation search, reference lists, single references, and GET/POST `/brief`; logs store endpoint, params, derived queries, and returned ids, not full chunk content.
- Last completed: Connected `ui_assets` to the API. `/assets/search` returns metadata-backed `AssetResult` records; `/assets/reference/{asset_id}` returns full asset metadata; `/brief` now defaults `asset_limit=6` and returns `asset_queries` plus `asset_suggestions` while keeping `automation_limit=0` by default.
- Last completed: Prepared repository for GitHub upload to `git@github.com:hsq2004815-oss/My-Own-DataBase.git`; rewrote root `README.md`, added `AGENT.md` and `AGENTS.md`, added `.gitignore` for caches/logs/temp brief artifacts, and added `.gitattributes` to keep text LF and mark DB/media/font artifacts binary.
- Verification: `python -m py_compile backend_api/app/main.py scripts/brief.py`; direct `search_assets_records('dynamic background', 5)` returned motion references; direct `build_brief_response(...)` returned asset ids and 0 automation chunks; HTTP `GET /assets/search?q=dynamic%20background&limit=5` returned motion_reference assets; CLI `python scripts/brief.py "做一个高级玻璃生态 AI dashboard，带动态背景和字体" --ui 8 --workflow 4 --automation 0 --assets 6` returned UI/workflow chunks, asset suggestions, and no automation chunks.
- Known traps: Root `runtime/db/*.db` files are still 0 bytes; usable DBs are under `runtime/db/sqlite/*`. Query derivation is still rule-based. Existing clients should not assume `/brief` saves artifacts or returns `artifact_path`.
- Next likely edit points: Add thumbnail/contact-sheet generation for screenshots/videos; review font/icon/UI-kit licenses before marking anything `direct_use`; optionally add richer asset tags so search can distinguish background style, component type, and motion feel. Improve exact phrase search if ranking still returns broad chunks; consider shared generic helpers for UI/workflow/automation/assets search if the API grows more domains.
- Last completed: Added a high-priority premium Web UI rules layer for `ui_design`: 7 wiki topics, 6 processed references, registry tags/source/file index entries, `/brief` query routing for premium/landing/video hero/portfolio/SaaS terms, and multi-term fallback ranking in `scripts/ui_design/search_ui.py`.
- Verification: `normalize_reference.py --check` validated 38 UI references; `make_chunks.py` created 228 chunks; `build_sqlite_index.py` rebuilt `runtime/db/sqlite/ui_design/ui_design_references.db` with FTS5; `search_ui.py` returned the new premium rules for premium cinematic, liquid glass video hero, and mixed Chinese/English queries; direct `build_brief_response` returned new premium chunk ids for the five requested validation tasks; `py_compile` passed for `backend_api/app/main.py`, `scripts/brief.py`, and `scripts/ui_design/search_ui.py`.
- Known traps: Legacy `design-*.md` brand topics remain intentionally untouched and many are still information-insufficient per `optimization_report.md`; they should stay secondary to premium rules until reviewed one-by-one.
- Last completed: Follow-up cleanup wrapped the 7 premium wiki topics into normal multi-line Markdown, added `user-distilled-premium-web-ui-prompts` to `registry/priority_sources.json`, verified files_index coverage, and tuned `search_ui.py` result diversity so long premium queries surface Liquid Glass, cinematic hero, typography/motion, and landing-section rule families instead of repeated chunks from one record.
- Verification: registry JSON parsed; files_index missing check returned none; UI pipeline reran with 38 references / 228 chunks / rebuilt SQLite; requested three `search_ui.py` queries returned the new premium rule families first; `py_compile` passed for `backend_api/app/main.py` and `scripts/ui_design/search_ui.py`.
- Last completed: Added `scripts/ui_assets/enrich_metadata.py` and enriched 46 screenshot + 13 video metadata records without moving raw assets. Generated 59 asset chunks under `domains/ui_assets/processed/chunks`, improved `/assets/search` query aliases for portfolio/liquid glass/motion/Chinese terms, and documented the asset enrichment workflow.
- Verification: `enrich_metadata.py --check` reported 59 target records; metadata/chunks JSON parsed; direct `search_assets_records` tests for dark cinematic portfolio background, liquid glass button kit, motion reference hero background, and `高级 简历 网站 动态背景` returned the intended screenshot/video references; `py_compile` passed for `backend_api/app/main.py` and `scripts/ui_assets/enrich_metadata.py`.
- Last completed: Extended ui_assets enrichment for `lottie-web-library-and-demos-b6a4ec55` without per-file indexing. Lottie metadata now has use_cases, implementation_notes, avoid_when, ai_summary, prompt_tags, and remains `review_required`. Generated 6 collection-level summary chunks: loading animation, micro interaction, hover motion, JSON web animation, animated icon, and implementation notes. Added `scripts/ui_assets/search_assets.py` and Chinese aliases for 小动画/微交互/加载动画/按钮动画/图标动画/Lottie 动画.
- Verification: Existing screenshot/video chunks are tracked in Git; enrich `--check` reported 60 metadata records and 65 chunks; enrich wrote 65 chunks plus all_asset_chunks; Lottie search tests returned the Lottie collection first for all four requested queries. Raw assets were not modified. Review-required collections remain not approved for direct commercial use.
- Last completed: Added Local Database API First Protocol to agent-facing docs. `AGENT.md`, `AGENTS.md`, `AGENT_USAGE.md`, `README.md`, and `backend_api/README.md` now state that local FastAPI is the default runtime source and GitHub is only backup/inspection/version sync. `scripts/brief.py` now defaults to UI payload `8/2/0/10` and performs `/health` before `/brief`.
- Verification: `python -m py_compile scripts/brief.py`; `git diff --check`.
- Last completed: Added Motion Asset Bootstrap Protocol for high-end UI/frontend/portfolio/landing tasks. Agents must supplement `/brief` with `/assets/search` for Lottie, micro-interaction, loading, hover, button/icon animation, motion reference, and hero background motion queries, then report asset/chunk ids, usage policy, inspiration/direct-use status, and CSS/SVG/Canvas/Lottie/video implementation path.
- Verification: `scripts/brief.py` now auto-runs those extra asset searches for matching high-end UI motion tasks and prints the no-direct-use fallback sentence when applicable.
- Last completed: Added Liquid Glass Web App UI Kit rules distilled from `D:\ai-ui-test\index.html` / AI Project Workspace. This is explicitly a Web App / SaaS Workspace / Dashboard component-system rule, not a landing page rule. New wiki topic: `domains/ui_design/wiki/topics/liquid-glass-web-app-ui-kit.md`; new reference: `implementation-liquid-glass-web-app-ui-kit`; new tags include `glass-workspace`, `liquid-glass-ui-kit`, `glass-form-controls`, `glass-modal`, `glass-toast`, `glass-tabs`, `glass-input`, `dark-neon-card`, `light-glass-dashboard`, `workspace-ui`, `ai-project-dashboard`, `premium-web-app`, and `micro-interaction-states`.
- Verification: UI pipeline validated 39 references, generated 234 chunks, rebuilt `runtime/db/sqlite/ui_design/ui_design_references.db`; search tests for Liquid Glass web app UI kit, AI Project Workspace, and Chinese 高级玻璃工作台 queries returned `implementation-liquid-glass-web-app-ui-kit` first; direct `build_brief_response` for an AI Project Workspace task returned the new chunk family in `ui_chunks`.
- Last completed: Added premium Video Hero Layout Patterns from `D:\Downloads\codex_video_hero_layout_patterns_prompt.txt`. New wiki topic: `video-hero-layout-patterns.md`; new references: `pattern-video-hero-layout-variants`, `pattern-two-panel-liquid-glass-hero`, `pattern-technical-education-hls-hero`, `pattern-premium-video-cta-and-navbar`, and `pattern-strict-grayscale-liquid-glass-hero`. These rules are for Hero / Landing Page / SaaS Homepage / Portfolio First Screen / video first screens, not Web App / Dashboard / Workspace component systems.
- Verification: UI pipeline validated 44 references, generated 264 chunks, rebuilt SQLite; search tests for video hero layout, two-panel Liquid Glass grayscale hero, technical education HLS hero, and Chinese equivalents returned the new video hero rule families first.
- Last completed: Added Premium UI Execution Quality Rules from `D:\Downloads\codex_premium_ui_execution_quality_prompt.txt`. New wiki topic: `premium-ui-execution-quality-rules.md`; new reference: `implementation-premium-ui-execution-quality-rules`; new registry tags/source/files_index entries; AGENT docs now require a UI execution self-check; `/brief` and `search_ui.py` route premium/visual-anchor/non-template queries to the execution layer.
- Verification: JSON parsed; `py_compile` passed for `backend_api/app/main.py`, `scripts/brief.py`, and `scripts/ui_design/search_ui.py`; `normalize_reference.py --check` validated 45 UI references; `make_chunks.py` generated 270 chunks; `build_sqlite_index.py` rebuilt `runtime/db/sqlite/ui_design/ui_design_references.db`; searches for premium UI execution, Chinese visual-anchor/non-template, light Liquid Glass workspace, and design self-check returned the new reference in the top results.
- Last completed: Added initial raw `domains/backend` corpus to Git as upcoming backend database material. The staged content includes 1606 actual files under `raw/github_projects` and intentionally excludes 11 nested `.git` metadata directories to avoid broken gitlink/submodule entries; those `.git` directories were moved to ignored `.omx/backend-git-metadata-backup-*` for local preservation.
- Verification: Preflight counted 1926 files / 33.4 MB including `.git`, 1606 files / 21.0 MB excluding `.git`; no files exceeded GitHub's 100 MB limit; secret scan found only README/test/example placeholders; `git diff --cached --raw` had no `160000` gitlink entries.
- Last completed: Generated backend GitHub project analysis artifacts for 13 projects under `domains/backend/raw/github_projects` without running projects, installing dependencies, writing rules/wiki, touching runtime SQLite, or modifying raw sources. New outputs are 13 `processed/cleaned_text/github_projects/*.analysis.md`, 13 `processed/metadata/github_projects/*.metadata.json`, 13 `processed/chunks/github_projects/*.chunks.json`, and `output/reports/github-projects-analysis-report.md`.
- Verification: Metadata/chunks JSON parsed successfully; no empty output files; all chunk content lengths are 300-800 characters; no generated path references include `.git`, `node_modules`, `venv`, `.venv`, or `__pycache__`. Backend schema is still missing from `common/schemas`; next phase should confirm analysis results before generating `backend_reference.schema.json`, backend README, rules, wiki, references JSON, SQLite, or API retrieval.
- Last completed: Backend Phase 2A created `common/schemas/backend_reference.schema.json`, `domains/backend/README.md`, 13 core files under `domains/backend/rules`, and `domains/backend/output/reports/backend-phase-2a-schema-readme-rules-report.md`. The rules cover engineering map, Agent usage, API design, database modeling, auth/permission, layered architecture, errors/logging, security, deployment/env, performance/stability, AI backend, GitHub project selection, and backend templates.
- Verification: `backend_reference.schema.json` parses as JSON; all 13 rules exist and include the required sections; README contains the mandated priority-read routing; no empty files; no copied hardcoded-looking secret patterns from first-phase `.env.example` findings; no `runtime/db/sqlite` changes; no wiki topic/pattern/checklist/template files or references JSON were generated. Next likely phase is wiki topics/patterns/checklists/templates/references JSON after user confirmation.
